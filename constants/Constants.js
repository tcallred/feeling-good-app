const cardSize = 120;
const cardMargin = 10;
const themeColor = '#0195d5';
const secondaryColor = '#FF9000';

export default {
    cardSize: cardSize,
    cardMargin: cardMargin,
    themeColor: themeColor,
    secondaryColor: secondaryColor
};